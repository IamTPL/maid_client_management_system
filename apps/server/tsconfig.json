{
  "compilerOptions": {
    /* --- Environment Settings --- */
    "target": "es2022", // Compile to modern JS (Node 16+)
    "lib": ["es2022"], // Modern JS libraries
    "module": "commonjs", // Very important: Express runs on CommonJS
    "moduleResolution": "node", // How Node.js resolves modules

    /* --- Build Settings --- */
    "rootDir": "src", // Our source code is here
    "outDir": "dist", // Compiled output goes here
    "sourceMap": true, // Needed for debugging

    /* --- Module Interop --- */
    "esModuleInterop": true, // Allows "import express from 'express'"
    "resolveJsonModule": true, // Allows importing .json files

    /* --- Strictness --- */
    "strict": true, // Enable all strict type-checking options (best practice)
    "skipLibCheck": true, // Skip type checking of declaration files (.d.ts)
    "forceConsistentCasingInFileNames": true // Ensure consistent file name casing
  },
  "include": ["src/**/*"], // Only compile code inside /src
  "exclude": ["node_modules"] // Do not compile node_modules
}
